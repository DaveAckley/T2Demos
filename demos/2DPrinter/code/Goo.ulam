/** Placeholder for cell guts.  Primary immediate purpose is that it's
    non-membrane content.

    \symbol Gu
    \color #757
    \symmetries all
    \radius 1
*/
element Goo : Content {
  Void behave() {
    
    // For now, just diffuse
    EventWindow ew;
    if (ew[1] is Empty) ew.swap(0,1);
  }
}
